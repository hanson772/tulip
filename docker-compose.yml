services:
  app:
    build: .
    ports:
      - "${PORT:-5000}:5000"
    environment:
      - PORT=5000
      - JWT_SECRET=${JWT_SECRET:-tulip_jwt_secret_key_change_in_production}
    volumes:
      - tulip-data:/app/data
    restart: unless-stopped

volumes:
  tulip-data:
